<table class="table table-striped">
  <thead>
    <tr>
			<th>Deacon</th>
			<th>Client</th>
      <th>Dates</th>
      <th>&nbsp;</th>
    </tr>
  </thead>
  <tbody>
    <% @past_assignments.each do |assignment| %>
      <tr>
				<td><%= link_to assignment.deacon.name, deacon_path(assignment.deacon) %></td>
				<td><%= link_to assignment.client.name, client_path(assignment.client) %></td>
        <td><%= assignment.start_date.strftime("%m/%d/%y") %> - <%= assignment.end_date.strftime("%m/%d/%y") %></td>
        <td>
					<%= link_to raw("Details"), assignment_path(assignment), :class => 'btn btn-mini' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= will_paginate @past_assignments, :previous_label => "Previous&nbsp;", :next_label => "&nbsp;Next" %>
